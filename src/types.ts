export type UserSignUpDetails = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    level: string;
}

export type UserDetails = {
    firstName: string;
    lastName: string;
    email: string;
    level: string;
}